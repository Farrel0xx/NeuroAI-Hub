# Core
streamlit==1.32.0

# TensorFlow latest (support Keras 3.x, fix batch_shape issue)
tensorflow==2.17.0  # Versi 2025 terbaru, support input config baru

# Atau tensorflow-cpu kalau mau lebih ringan (no GPU warning)
# tensorflow-cpu==2.17.0

# OpenCV headless (fix libGL error sebelumnya)
opencv-python-headless==4.10.0.84

# Lainnya
Pillow==10.4.0
numpy==2.0.2
scipy==1.14.1

# Gemini kalau masih dipake
google-genai==0.8.0
