# Core web framework for the app
streamlit==1.32.0

# Deep learning (CPU version to avoid GPU warnings on Streamlit Cloud)
tensorflow-cpu==2.15.0

# Image processing (headless REQUIRED for no libGL error)
opencv-python-headless==4.9.0.80

# Image handling
Pillow==10.2.0

# Numerical and scientific
numpy==1.26.4
scipy==1.11.4

# Google Gemini API (for genai features like explanation or multimodal)
google-genai==0.5.0
